import { stack } from "@btst/stack"
{{{adapterImport}}}
{{#if backendImports}}
{{{backendImports}}}
{{/if}}

{{#if adapterSetup}}
{{{adapterSetup}}}
{{/if}}
const myStack = stack({
	basePath: "/api/data",
	plugins: {
{{#if backendEntries}}
{{{backendEntries}}}
{{else}}
		// Add backend plugins here.
{{/if}}
	},
	{{{adapterStackLine}}}
})

export const { handler, dbSchema } = myStack
