/** Legacy example URL. Management data must render through request authorization. */
import { redirect } from "next/navigation"

export default function LegacyManagementPage() {
	redirect("/pages/forms")
}
