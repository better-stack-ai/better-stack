import { toReactRouterHandlers } from "@btst/stack/react-router"
import { handler } from "{{alias}}lib/stack"

// React Router's build can't strip destructured exports from route modules,
// so assign loader/action individually instead of destructuring the export.
const handlers = toReactRouterHandlers(handler)
export const loader = handlers.loader
export const action = handlers.action
